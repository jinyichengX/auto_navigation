import { GuideStep } from '../types';

export const simulationSteps: GuideStep[] = [
  {
    step: 1,
    title: 'URDF / Xacro：机器人描述文件',
    description: '一切从 URDF 开始。URDF（Unified Robot Description Format）是描述机器人几何结构、关节、传感器的 XML 文件。\n\nXacro 是 URDF 的宏增强版，你的 fishbot.xacro 通过 <xacro:include> 组合了 base、imu、laser、wheel 等子模块，通过 <xacro:macro> 来复用代码。\n\nLaunch 文件中的 Command([\'xacro \', model_path]) 会把 .xacro 展开成完整的 URDF 字符串，然后通过 ParameterValue 注入到 ROS 2 参数服务器，即 /robot_description 参数。',
    addNodes: ['xacro', 'robot_description'],
    removeNodes: [],
    highlightConnections: [],
  },
  {
    step: 2,
    title: 'robot_state_publisher：TF 广播器',
    description: 'robot_state_publisher 读取 /robot_description 中的 URDF，解析所有 link 和 joint 的定义。\n\n对于 fixed（固定）关节，它广播 /tf_static（只发一次）。\n对于 continuous（连续，如轮子）等非固定关节，它需要 /joint_states 来知道当前角度，然后广播 /tf。\n\n注意：当前还没有 /joint_states，所以非固定关节的 TF 还无法计算。',
    addNodes: ['robot_state_publisher'],
    removeNodes: [],
    highlightConnections: [],
  },
  {
    step: 3,
    title: 'joint_state_publisher：关节状态发布',
    description: 'joint_state_publisher 扫描 URDF 中的所有非固定关节，发布 /joint_states。\n\n在 Gazebo 仿真模式中，它只是提供一个初始状态（所有关节角度为 0.0），让 robot_state_publisher 能正常工作。实际运动由 Gazebo 插件驱动。\n\n现在 robot_state_publisher 可以读取 /joint_states，完成 TF 树了。',
    addNodes: ['joint_state_publisher'],
    removeNodes: [],
    highlightConnections: [],
  },
  {
    step: 4,
    title: 'ROS 2 DDS 通信网络',
    description: '以上所有组件都在 ROS 2 DDS 网络上通信。\n\nDDS（Data Distribution Service）是 ROS 2 的底层通信协议。节点通过"话题（Topic）"发布和订阅消息，底层自动完成发现、序列化、传输。\n\n关键概念：ROS 2 节点之间可以直接通信，不需要中间人。一个节点发布 /joint_states，所有订阅它的节点自动收到。',
    addNodes: [],
    removeNodes: [],
    highlightConnections: [],
  },
  {
    step: 5,
    title: 'Gazebo Fortress：仿真世界',
    description: '现在引入 Gazebo。Gazebo 是一个独立的仿真器，有自己的世界文件（.sdf）、物理引擎、渲染器。\n\n关键概念：Gazebo 内部的通信使用 Gazebo Transport（零拷贝消息系统），不是 ROS 2 的 DDS！这意味着 ROS 2 节点不能直接和 Gazebo 内部插件通信——需要桥梁。\n\nGazebo 加载 world1.sdf 世界文件，启动物理引擎（ODE/Bullet/DART）。',
    addNodes: ['gz_sim', 'gz_transport_layer', 'physics_engine'],
    removeNodes: [],
    highlightConnections: [],
  },
  {
    step: 6,
    title: 'Spawn Robot：把机器人放进去',
    description: 'ros_gz_sim create 节点（spawn_robot）从 /robot_description 读取 URDF，内部转换为 SDF 格式，然后在 Gazebo 世界中以名称 fishbot 生成机器人模型。\n\nGazebo 侧会创建 /model/fishbot/* 系列话题（tf、odom、pose 等）。\n\n注意：此时机器人已在 Gazebo 中，但没有人驱动它——还没有加载运动控制插件。',
    addNodes: ['spawn_robot'],
    removeNodes: [],
    highlightConnections: [],
  },
  {
    step: 7,
    title: '方案 A：DiffDrive 插件（简单方案）',
    description: '在 URDF 的 <gazebo> 标签中加载 libignition-gazebo-diff-drive-system.so（DiffDrive 插件）。\n\n这是一个纯 Gazebo 插件，运行在 Gazebo 内部：\n1. 接收速度指令（gz.msgs.Twist）\n2. 差速解算（v_x, ω_z → 左右轮速度）\n3. 驱动四个轮子关节\n4. 读取关节实际转角 → 反向推算里程计\n5. 发布里程计（gz.msgs.Odometry）和 TF（/model/fishbot/tf）\n\n所有通信都在 Gazebo Transport 上！ROS 2 无法直接访问。',
    addNodes: ['diff_drive_plugin'],
    removeNodes: [],
    highlightConnections: [],
  },
  {
    step: 8,
    title: 'ros_gz_bridge：翻译桥梁（方案 A 必须！）',
    description: '因为 DiffDrive 插件在 Gazebo Transport 上通信，ROS 2 需要 ros_gz_bridge 来翻译：\n\n下行（ROS → GZ）：\n- teleop 发 /cmd_vel（ROS DDS）→ bridge 翻译 → gz.msgs.Twist（Gazebo Transport）→ DiffDrive 插件\n\n上行（GZ → ROS）：\n- DiffDrive 发 /odom（gz.msgs.Odometry）→ bridge 翻译 → /odom（nav_msgs/Odometry，ROS DDS）\n- DiffDrive 发 /model/fishbot/tf（gz.msgs.Pose_V）→ bridge 翻译 → /tf（tf2_msgs/TFMessage，ROS DDS）\n\n橙色虚线表示跨边界通信。',
    addNodes: ['ros_gz_bridge', 'teleop'],
    removeNodes: [],
    highlightConnections: [],
  },
  {
    step: 9,
    title: '方案 A 完整数据流回顾',
    description: '方案 A 的完整数据流：\n\nteleop →(ROS DDS)→ /cmd_vel →(bridge 翻译)→ gz.msgs.Twist → DiffDrive 插件 → 差速解算 → 驱动关节 → 物理引擎\n                                                                       → 反推里程计 → gz.msgs.Odometry →(bridge 翻译)→ /odom(ROS DDS)\n                                                                       → gz.msgs.Pose_V →(bridge 翻译)→ /tf(ROS DDS)\n\nros_gz_bridge 是必须的，因为 DiffDrive 插件在 Gazebo 里面。',
    addNodes: [],
    removeNodes: [],
    highlightConnections: [],
  },
  {
    step: 10,
    title: '方案 B：引入 ros2_control（首先加载 gazebo_ros2_control 插件）',
    description: '现在切换到 ros2_control 方案。\n\n首先，在 URDF 的 <gazebo> 标签中加载 libgazebo_ros2_control.so。\n\n这个插件不直接控制机器人！它只是 gazebo_ros2_control 框架在 Gazebo 侧的"硬件接口层"。它读取 URDF 中 <ros2_control> 标签，注册关节的 velocity 命令接口和 position/velocity 状态接口。\n\n同时从 URDF 中移除（注释掉）DiffDrive 插件。',
    addNodes: ['gz_ros2_control_plugin'],
    removeNodes: ['diff_drive_plugin'],
    highlightConnections: [],
  },
  {
    step: 11,
    title: 'controller_manager：大脑',
    description: 'controller_manager 是 ros2_control 的核心管理节点，运行在 ROS 2 侧。\n\n功能：\n1. 读取 URDF 中的硬件接口定义\n2. 根据 YAML 配置文件加载 controller\n3. 管理 controller 的生命周期（加载/激活/停用/卸载）\n4. 在每个控制周期中：读取硬件状态 → 传给 controller → 接收控制指令 → 写回硬件\n\n注意：现在还没有加载任何 controller，机器人还是不会动。',
    addNodes: ['controller_manager'],
    removeNodes: [],
    highlightConnections: [],
  },
  {
    step: 12,
    title: 'diff_drive_controller：差速驱动控制器',
    description: 'diff_drive_controller 由 controller_manager 加载，运行在 ROS 2 侧。\n\n它的功能（与 DiffDrive 插件完全相同）：\n1. 订阅 /cmd_vel（直接 ROS 2 DDS！）\n2. 差速解算（v_x, ω_z → v_left, v_right）\n3. write() 设置关节速度 → 通过 GazeboSystem 硬件接口 → Gazebo 关节\n4. read() 读取关节状态 ← 通过 GazeboSystem 硬件接口 ← Gazebo 物理引擎\n5. 反向推算里程计 → 直接发布 /odom（ROS 2 DDS！）\n6. 直接发布 /tf（ROS 2 DDS！）\n\n关键区别：所有话题通信都在 ROS 2 DDS 侧！与 Gazebo 的交互只通过 GazeboSystem 硬件接口的 write/read，不经过 Gazebo Transport 话题！',
    addNodes: ['diff_drive_controller', 'hardware_interface_gz'],
    removeNodes: [],
    highlightConnections: [],
  },
  {
    step: 13,
    title: '方案 B 完整数据流：不再需要 ros_gz_bridge！',
    description: '方案 B 的完整数据流：\n\nteleop →(ROS DDS)→ /cmd_vel → diff_drive_controller → 差速解算 → write(关节速度) → GazeboSystem 硬件接口 → Gazebo 关节 → 物理引擎\n                                                       ← read(关节状态) ← GazeboSystem 硬件接口 ←──┘\n                                                       → 反推里程计 → 发布 /odom (直接 ROS DDS)\n                                                       → 发布 /tf (直接 ROS DDS)\n\n/odom 和 /tf 直接从 diff_drive_controller 发布到 ROS 2 DDS，不经过 Gazebo Transport，不需要 bridge！\n\nros_gz_bridge 中的 /cmd_vel、/odom、/tf 三条桥接都可以删掉！',
    addNodes: [],
    removeNodes: ['ros_gz_bridge'],
    highlightConnections: [],
  },
  {
    step: 14,
    title: '总结：方案 A vs 方案 B',
    description: '方案 A（DiffDrive 插件）：\n- 差速解算和里程计在 Gazebo 内部完成\n- 必须用 ros_gz_bridge 桥接 /cmd_vel、/odom、/tf\n- 配置简单（只需 URDF <plugin> 标签 + bridge 参数）\n- 缺点：耦合在 Gazebo，真机无法复用\n\n方案 B（ros2_control）：\n- 差速解算和里程计在 ROS 2 侧的 diff_drive_controller 完成\n- 不需要 ros_gz_bridge 桥接 /cmd_vel、/odom、/tf\n- 与 Gazebo 只通过硬件接口的 write/read 交互\n- 优点：controller 代码在真机和仿真间完全复用，只换硬件接口实现',
    addNodes: [],
    removeNodes: [],
    highlightConnections: [],
  },
];

export const realRobotSteps: GuideStep[] = [
  {
    step: 1,
    title: '真机模式：同样的上层，不同的底层',
    description: '真机模式中，URDF、robot_state_publisher、joint_state_publisher 不变。\n\n核心变化：没有 Gazebo，取而代之的是真实硬件和真实硬件接口。\n\nxacro 展开 URDF → /robot_description → robot_state_publisher 广播 TF。',
    addNodes: ['xacro', 'robot_description', 'robot_state_publisher', 'joint_state_publisher'],
    removeNodes: [],
    highlightConnections: [],
  },
  {
    step: 2,
    title: 'ros2_control 在真机上的架构',
    description: 'controller_manager 和 diff_drive_controller 完全不变！\n\ndiff_drive_controller 仍然：\n- 订阅 /cmd_vel（ROS 2 DDS）\n- 差速解算\n- write(速度指令)\n- read(编码器状态)\n- 发布 /odom（ROS 2 DDS）\n- 发布 /tf（ROS 2 DDS）\n\n唯一变化：硬件接口从 GazeboSystem 换成 DiffBotSystemHardware。\n\n这就是 ros2_control 的核心价值——上层 controller 代码在仿真和真机间完全复用！',
    addNodes: ['controller_manager', 'diff_drive_controller', 'teleop'],
    removeNodes: [],
    highlightConnections: [],
  },
  {
    step: 3,
    title: '真实硬件接口：操作真实物理世界',
    description: '硬件接口从 GazeboSystem（虚拟关节）换成 DiffBotSystemHardware（真实硬件）：\n\nwrite(速度) → 通过串口/CAN → 电机驱动板 → PWM → 真实电机转动\nread(状态) ← 通过串口/CAN ← 编码器 ← 真实轮子转角\n\n不需要 Gazebo、不需要 ros_gz_bridge、不需要 spawn_robot。\n物理引擎就是真实世界的物理学。',
    addNodes: ['hardware_interface_real', 'real_motor_driver', 'real_encoder'],
    removeNodes: [],
    highlightConnections: [],
  },
  {
    step: 4,
    title: '仿真 vs 真机对比总结',
    description: '        仿真模式                        真机模式\n硬件接口  GazeboSystem                  DiffBotSystemHardware\n物理层   Gazebo 物理引擎               真实世界物理\n通信     Gazebo Transport（部分）      全部 ROS 2 DDS\n桥接     ros_gz_bridge（方案A需要）     不需要\n控制器   diff_drive_controller          diff_drive_controller（完全相同！）\n\n关键：diff_drive_controller 的代码一行不改，从仿真切到真机只要换硬件接口实现！',
    addNodes: [],
    removeNodes: [],
    highlightConnections: [],
  },
];

export const multiRobotSteps: GuideStep[] = [
  {
    step: 1,
    title: '两个机器人，一个 Gazebo',
    description: '假设同时加载 fishbot 和 fishbot2 两个机器人。\n\nGazebo 只有一个实例，物理引擎也只有一个。\n\n每个机器人需要：\n- 独立的 xacro/URDF 描述\n- 独立的 robot_state_publisher 和 joint_state_publisher\n- 独立的 controller_manager 和 diff_drive_controller\n- 独立的 GazeboSystem 硬件接口\n\nGazebo Transport 上，话题按 /model/{name}/... 区分。',
    addNodes: ['gz_sim', 'gz_transport_layer', 'physics_engine', 'spawn_robot'],
    removeNodes: [],
    highlightConnections: [],
  },
  {
    step: 2,
    title: '多机器人 + ros2_control：各有一条独立链路',
    description: '每个机器人有独立的 ros2_control 链路，都在 ROS 2 DDS 侧：\n\nfishbot 链路：\n/cmd_vel → diff_drive_controller_fishbot → GazeboSystem(fishbot) → Gazebo 关节\n\nfishbot2 链路：\n/cmd_vel2 → diff_drive_controller_fishbot2 → GazeboSystem(fishbot2) → Gazebo 关节\n\ncontroller_manager 为每个机器人加载独立的 controller 实例。\n\n不需要 ros_gz_bridge！所有 /cmd_vel、/odom、/tf 都在 ROS 2 DDS 侧。',
    addNodes: ['xacro', 'robot_description', 'robot_state_publisher', 'joint_state_publisher', 'teleop', 'controller_manager', 'diff_drive_controller', 'hardware_interface_gz', 'gz_ros2_control_plugin'],
    removeNodes: [],
    highlightConnections: [],
  },
  {
    step: 3,
    title: '多机器人 Gazebo Transport 话题隔离',
    description: 'Gazebo Transport 上，每个机器人的话题自动按 /model/{name}/ 隔离：\n\n/model/fishbot/pose\n/model/fishbot/tf\n/model/fishbot/odom\n/model/fishbot2/pose\n/model/fishbot2/tf\n/model/fishbot2/odom\n\n如果用 DiffDrive 方案，需要为每个机器人配独立的 ros_gz_bridge：\n- /cmd_vel → /model/fishbot/cmd_vel\n- /cmd_vel2 → /model/fishbot2/cmd_vel\n\n这也是 ros2_control 的另一个优势：不需要管理每个机器人的 bridge 配置。',
    addNodes: [],
    removeNodes: [],
    highlightConnections: [],
  },
];
